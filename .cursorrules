我需要训练一个 56 维度的 transformer 向量大小判别器。

额外信息：

1. 向量每个维度一定是正整数且不超过 100
2. 50 维向量中最多只会同时有 3 个维度不为 0
3. 如果 A > B 的概率是 p，则 A 的任意维度 + 任意正值 > B 的概率要 >= p
4. 不同维度对向量判别有不同“贡献”，有的贡献大，有的贡献小，在贡献差距悬殊时可以通过贡献判断；但是在贡献接近时，情况会复杂很多不能简单判断。
5. 有时候对向量的小幅度修改可能会大幅改变预测结果

输入数据格式：

单个 csv，前 112 列为向量 L，后 112 列为向量 R，最后一列为标签，标签为 L 或 R 代表更大的向量。
